module quaff_asserts_m
    use acceleration_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use amount_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use angle_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use area_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use burnup_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use convective_heat_transfer_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use density_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use dynamic_viscosity_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use energy_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use energy_per_amount_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use energy_per_temperature_amount_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use enthalpy_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use fluence_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use force_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use fracture_toughness_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use length_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use mass_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use mass_rate_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use molar_mass_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use power_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use pressure_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use specific_heat_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use speed_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use stress_intensity_factor_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use temperature_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use thermal_conductivity_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use thermal_expansion_coeffecient_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use time_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use volume_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
    use frequency_asserts_m, only: &
            assert_equals, &
            assert_equals_within_absolute, &
            assert_equals_within_relative
end module
